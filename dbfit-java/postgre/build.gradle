description = 'DBFit PostGreSQL engine'

dependencies {
  compile project(':dbfit-core')
  compile group: 'postgresql', name: 'postgresql', version:'9.1-901.jdbc4'
}

test.doFirst {
  dbdeploy('org.postgresql.Driver', 'jdbc:postgresql:dbfit', 'dbfit', 'dbfit')
}
